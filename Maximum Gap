class Solution {
public:
    int maximumGap(vector<int>& nums) {
        sort(nums.begin(),nums.end());
        int n=nums.size();
        int maxlen=0;
        for(int i=0;i<n-1;i++){
            
                maxlen=max(maxlen,nums[i+1]-nums[i]);
            }

        return maxlen;
    }
};
